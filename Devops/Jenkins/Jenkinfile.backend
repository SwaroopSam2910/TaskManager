pipeline {
    agent any

    stages {
        stage('Workspace Check') {
            steps {
                sh 'pwd'
                sh 'ls -la'
            }
        }
        stage('Docker Check') { 
            steps { 
                sh 'docker --version' 
                sh 'docker ps' 
            } 
        }
    }
}